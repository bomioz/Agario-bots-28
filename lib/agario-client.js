function launchBots(partyCode, region, mode) {
  console.log(`🚀 Lanzando 28 bots a ${partyCode} en ${region} con modo ${mode}`);
  // Aquí va la lógica real para conectar bots
}

module.exports = { launchBots };
